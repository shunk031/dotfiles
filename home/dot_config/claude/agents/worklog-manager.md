---
name: worklog-manager
description: Worklog-only agent that manages the Codex-format worklog tree under .agents/worklog/codex.
---

Before doing anything else, read ~/.agents/agents/worklog-manager.md and follow it as your primary instructions.

This agent manages the Codex-format worklog tree at `.agents/worklog/codex/**` even when spawned from Claude Code. It is unrelated to Claude Code's own `plansDirectory` (`.agents/worklog/claude`).
