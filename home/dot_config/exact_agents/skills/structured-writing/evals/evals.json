{
  "version": 1,
  "skill": "structured-writing",
  "evals": [
    {
      "id": "organize-operating-instructions",
      "prompt": "Rewrite these notes as concise operating instructions: install the hook in a new clone; the hook evaluates changed skills; authentication failure must stop the commit; use SKIP=agent-skill-eval only for emergencies; the static validator must still run; do not run model evaluation in CI.",
      "should_trigger": true,
      "assertions": [
        "The response presents hook installation before the commit-time behavior it enables.",
        "The response groups evaluation failures and the emergency bypass without weakening static validation.",
        "The response preserves the exact SKIP=agent-skill-eval value and the static-validation constraint.",
        "The response keeps the no-model-evaluation-in-CI rule distinct from local hook behavior."
      ]
    },
    {
      "id": "repair-flat-supporting-list",
      "prompt": "Turn these notes into an easy-to-scan bullet list without changing their meaning: Skill evaluation protects quality. It runs only for changed skills. Successful results are cached. Network failures block commits. Documentation changes follow a separate review process.",
      "should_trigger": true,
      "assertions": [
        "The response nests changed-skill scope, caching, and network-failure behavior under skill evaluation.",
        "The response keeps the documentation review process as an independent topic.",
        "The response preserves every original claim."
      ]
    },
    {
      "id": "machine-readable-output",
      "prompt": "Return only this JSON object with keys in the same order: {\"status\":\"ok\",\"count\":2}",
      "should_trigger": false,
      "assertions": [
        "The response contains only the requested valid JSON object.",
        "The key order and values are unchanged."
      ]
    }
  ]
}
