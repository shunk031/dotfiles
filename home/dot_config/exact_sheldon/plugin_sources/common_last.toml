# Reuse a dump newer than one day; older dumps get a full security and freshness check.
[plugins.compinit]
inline = '''
() {
    setopt localoptions extendedglob
    autoload -Uz compinit
    _compinit_dump="${ZDOTDIR:-$HOME}/.zcompdump"
    if [[ -n ${_compinit_dump}(#qN.mh-24) ]]; then
        compinit -C
    else
        compinit
    fi
    unset _compinit_dump
}
'''

# zsh-syntax-highlighting must be the last eagerly sourced plugin so it can
# wrap every widget and hook defined by the eager plugins above. Deferred
# plugins remain queued for later initialization by zsh-defer.
[plugins.zsh-syntax-highlighting]
github = 'zsh-users/zsh-syntax-highlighting'
apply = ['source']
